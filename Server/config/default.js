const config = {
    port: 4443, //server
    database: {
        DATABASE: 'todolist',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;